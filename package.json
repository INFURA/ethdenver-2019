{
  "devDependencies": {
    "docz": "^0.13.7",
    "docz-theme-default": "^0.13.7"
  },
  "scripts": {
    "build": "yarn docz build; cp -R public .docz/dist; cd .docz && tar -cvzf ../dist.tgz dist",
  },
  "dependencies": {
    "remark-emoji": "^2.0.2"
  }
}
